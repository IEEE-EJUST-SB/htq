# Copy to .env for Firebase emulator local testing (do not commit .env)
RESEND_API_KEY=
RESEND_FROM=HTQ <onboarding@resend.dev>
PUBLIC_SITE_URL=http://127.0.0.1:5000
CORS_ORIGINS=*
STAGE2_TOKEN_TTL_DAYS=30
